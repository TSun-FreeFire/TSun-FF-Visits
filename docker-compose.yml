version: "3.8"

services:
  tsun-ff-infoxvisits:
    build: .
    container_name: tsun-ff-infoxvisits
    restart: always
    ports:
      - "127.0.0.1:8076:8076"
    environment:
      - PYTHONUNBUFFERED=1
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8076/')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
